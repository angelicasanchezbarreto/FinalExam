/* import org.testng.Assert;
import org.testng.annotations.Test; */

public class AppSystemTest {
    /* @Test
    public void TestCaseRegister() {
        var projectSystem = ProjectSystem.getInstance();
        var newUser = new User("lica","apa");
        projectSystem.register("lica", newUser);
        Assert.assertEquals(projectSystem.currentUser.getUsername(), "lica");
        Assert.assertEquals(projectSystem.currentUser.getPassword(), "apa");
    }

    @Test
    public void logout() {
        var projectSystem = ProjectSystem.getInstance();
        projectSystem.logout();
        Assert.assertEquals(projectSystem.currentUser, null);
    }

    @Test(threadPoolSize = 500)
    public void TestCaseAddSensor(){
        var projectSystem = ProjectSystem.getInstance();
        projectSystem.addSensor();
        Assert.assertEquals(projectSystem.sensors.get(1).getID(), 1);
    } */
}
